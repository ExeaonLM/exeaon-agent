import { ResearchWarRoom } from "#/components/features/conversation/research-war-room";

/** Right-panel tab hosting the live Research war-room. */
export default function ResearchTab() {
  return <ResearchWarRoom />;
}
