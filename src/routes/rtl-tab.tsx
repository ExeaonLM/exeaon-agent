import { RtlWaveformViewer } from "#/components/features/conversation/rtl-waveform-viewer";

/** Right-panel tab hosting the live RTL waveform viewer (Computing field). */
export default function RtlTab() {
  return <RtlWaveformViewer />;
}
