import { RoboticsViewer } from "#/components/features/conversation/robotics-viewer";

/** Right-panel tab hosting the live MuJoCo robotics 3D viewer. */
export default function RoboticsTab() {
  return <RoboticsViewer />;
}
