import { SwarmGraph } from "#/components/features/conversation/swarm-graph";

/** Right-panel tab hosting the live cyber-swarm node graph. */
export default function SwarmTab() {
  return <SwarmGraph />;
}
